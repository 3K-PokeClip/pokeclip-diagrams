# PokeClip 설계 다이어그램

방송이 끝나기 전에 하이라이트 클립을 유튜브에 올리는 서비스, PokeClip의 설계 문서입니다.

**→ https://3k-pokeclip.github.io/pokeclip-diagrams/**

외부 의존성 없는 self-contained HTML입니다. 파일 하나만 열면 그대로 보이고, 별도 빌드가 필요 없습니다.

| | 문서 | 내용 |
|---|---|---|
| ① | [유스케이스](01-usecase.html) | 액터 6(사람 2 · 외부 시스템 4) · 유스케이스 32 · 그룹 6 |
| ② | [IA · 정보 구조](02-ia.html) | depth 1 4갈래 · 노드 165 · UC 배지 50개 |
| ③ | [서비스 아키텍처](03-service-architecture.html) | 서비스 블록 21 · 접점 4 · 외부 4 · 연결 45 · 흐름 5 |
| ④ | [시스템 아키텍처](04-system-architecture.html) | 배포 프로그램 7 · 저장소 4 · 외부 4 · 연결 20 — ③ 블록을 실행 프로그램·프로토콜·저장소로 |
| ⑤ | 클라우드 아키텍처 (AWS) | 예정 |

## 개발 단계

세 문서 상단의 `P1` `P2` `P3` 칩을 누르면 **그 단계에 만드는 것만** 남습니다. 여러 개를 함께 켤 수 있고, 셋 다 켜면 전체와 같습니다.

| | 단계 | 기준 | UC |
|---|---|---|:-:|
| **P1** | 핵심 루프 | 스트리머 혼자 방송 중에 클립을 올린다 | 12 |
| **P2** | 협업 · 자산 | 편집자를 붙이고 방송이 끝나도 쓴다 | 13 |
| **P3** | 상용화 | 돈을 받고 모르는 사람이 가입해 쓴다 | 7 |

단계는 **①의 유스케이스에만 매기고 ②·③·④는 UC 번호로 물려받습니다.** 한 곳만 고치면 문서들이 같이 움직입니다. 화면이나 블록이 여러 UC에 걸치면 **가장 이른 단계**를 씁니다 — 그때 만들기 시작하고 기능 일부는 나중에 붙는다는 뜻입니다.

예외가 하나 있습니다. `AI 자막`은 UC-22(클립 편집, P1)에 속하지만 자막 기능만 P2에 붙이므로, ①의 UC-22 하위 배지 3개 · ②의 자막 패널 · ③의 AI 자막 블록을 모두 P2로 직접 지정했습니다.

**④는 단계 분해가 다르게 나옵니다.** 자동 감지 핵심 루프가 7개 프로그램 전부를 건드리므로, **시스템 골격은 P1에서 사실상 완성**됩니다. 그래서 ④에서 `P2`를 누르면 새로 켜지는 게 없습니다(협업·승인·AI 자막 등은 기존 프로그램 **내부** 기능 확장이라 새 실행 단위가 아님). 시스템 층에서 새로 붙는 요소는 `P3`의 결제(PG) 연동뿐입니다 — "MVP라고 인프라를 반만 세울 수는 없다"는 사실을 그대로 보여줍니다.

## 보는 법

네 문서 모두 큰 캔버스라, 화면에 맞춰 축소된 상태로 열립니다.

| 동작 | 방법 | ① | ② | ③ | ④ |
|---|---|:-:|:-:|:-:|:-:|
| 화면 이동 | 빈 곳 드래그 · 휠 스크롤 | ○ | ○ | ○ | ○ |
| 확대 / 축소 | Ctrl + 휠 (커서 기준) · `＋` `−` | ○ | ○ | ○ | ○ |
| 화면에 맞추기 | `맞춤` | ○ | ○ | ○ | ○ |
| 원래 크기 | `1:1` | — | ○ | — | — |
| 관련 요소만 보기 | 액터 · 유스케이스 · 서비스 블록 · 프로그램 · 흐름 칩 클릭 | ○ | — | ○ | ○ |
| 개발 단계만 보기 | `P1` `P2` `P3` 칩 클릭 (여러 개 동시) | ○ | ○ | ○ | ○ |
| 강조 해제 | 재클릭 · 빈 곳 클릭 · `Escape` | ○ | ○ | ○ | ○ |

②는 계층 구조를 통째로 보는 문서라 관계 강조는 없고, 단계 필터만 있습니다.

## 문서 사이의 규칙

- ②·③·④는 ①의 유스케이스 번호(`UC-01`~`UC-32`)로 ①을 참조합니다. 번호가 바뀌면 함께 고쳐야 합니다.
- ③의 서비스 블록은 ①의 유스케이스 32개를 **빠짐없이** 나눠 가집니다. 한 유스케이스가 여러 블록에 걸치는 경우(예: `UC-22` 클립 편집 = 편집 + 렌더 + AI 자막)는 서비스 경계를 가로지르는 것이라 의도한 것입니다.
- ④는 ③의 서비스 블록 21개를 **실제 실행되는 프로그램 7개로 N:M 매핑**합니다(예: `송출 수신` = 미디어 서버 + Core API). ③이 "무엇을 하는가"(논리 기능)라면 ④는 "무엇이 돌면서 어떤 프로토콜로 주고받는가"(실행 단위·전송)입니다. 감지는 ③·④ 모두 **채팅 기반 감지 서버**의 몫이고 플러그인은 핫키 신호만 보냅니다.
- 서비스 설명에 없는 기능·컴포넌트는 임의로 넣지 않습니다. 판단이 필요한 항목은 문서 안에 `TBD`로 남깁니다.

## 미정 (TBD)

- 종량 과금 기준 — 계측 대상·단가 미정
- 무료 체험 · 무료 티어 유무
- 알림 수신 채널 (웹 푸시 · 이메일)
- 계정 삭제 시 클립 · 편집본 · VOD 처리
- 오픈소스 라이선스 고지 — OBS · FFmpeg 계열 의존성 확인 후 확정

## 관련 저장소

- [3K-PokeClip/PokeClip-architecture](https://github.com/3K-PokeClip/PokeClip-architecture) — 팀 아키텍처 문서 · ADR · 기능 명세서
